{
  "name": "vedantu-talent-desk-pilot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test": "node --test --test-isolation=none tests/*.test.mjs",
    "db:local": "wrangler d1 migrations apply vedantu_talent_desk_db --local && wrangler d1 execute vedantu_talent_desk_db --local --file seed.sql",
    "db:remote": "wrangler d1 migrations apply vedantu_talent_desk_db --remote && wrangler d1 execute vedantu_talent_desk_db --remote --file seed.sql",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.92.0"
  }
}
