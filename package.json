{
  "name": "vedantu-talent-desk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test": "node --test --test-isolation=none tests/*.test.mjs",
    "db:local": "wrangler d1 migrations apply vedantu_talent_desk_db --local",
    "db:remote": "wrangler d1 migrations apply vedantu_talent_desk_db --remote",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.92.0"
  }
}
